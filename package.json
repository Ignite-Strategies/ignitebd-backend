{
  "name": "ignite-activation-api",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "node index.js",
    "build": "prisma generate && prisma migrate deploy && prisma db push --accept-data-loss",
    "start": "node index.js",
    "db:generate": "prisma generate",
    "db:push": "prisma db push",
    "db:migrate": "prisma migrate dev",
    "db:studio": "prisma studio",
    "postinstall": "prisma generate"
  },
  "dependencies": {
    "cookie-session": "^2.1.0",
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "firebase-admin": "^13.5.0",
    "multer": "^2.0.2",
    "openai": "^6.6.0",
    "socket.io": "^4.8.1"
  },
  "devDependencies": {
    "@prisma/client": "^6.18.0",
    "prisma": "^6.18.0"
  }
}
